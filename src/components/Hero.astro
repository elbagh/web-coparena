---
import { event } from "../data/event";
---

<section class="hero" aria-labelledby="hero-title">
  <img
    class="hero-art"
    src="/assets/copa-arena-hero-watercolor.png"
    alt="Partido de vóley playa al atardecer en la arena, tratado en acuarela"
    loading="eager"
    decoding="async"
  />
  <div class="hero-overlay" aria-hidden="true"></div>

  <div class="hero-content">
    <p class="eyebrow hero-eyebrow">{event.location}</p>
    <p class="hero-name">{event.name}</p>
    <h1 id="hero-title" class="hero-dates">
      <span class="date-line">{event.dateStart}</span>
      <span class="date-line date-line-end"><span class="date-dash" aria-hidden="true">—</span>{event.dateEnd}</span>
    </h1>
    <p class="hero-claim">{event.claim}</p>
    <div class="hero-actions">
      <a class="button button-primary" href="#jugar">Quiero jugar</a>
      <a class="button button-secondary" href="#venir">Quiero venir</a>
    </div>
  </div>
</section>
