# Programación del torneo 2026 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que la clasificación sepa expresar «2 plazas directas aquí, 3 allí y 1 de repesca entre dos grupos», que se pinte con colores en el panel y en `/torneo/`, y dejar el torneo 2026 programado con sus cinco tardes.

**Architecture:** Una migración de solo `ALTER` añade `torneo_grupos.clasifican`, `torneo_grupos.en_repesca` y `torneo_fases.repesca`. Una función pura nueva, `functions/_lib/clasificados.ts`, decide quién pasa y en qué orden se siembra; **es la única fuente** tanto de los colores como de las semillas del cuadro, para que la tabla y el cuadro no puedan discrepar. `torneo-vista.ts` la llama y expone el resultado; el panel y la página pública solo pintan.

**Tech Stack:** Astro 5 (salida estática), Cloudflare Workers + D1, TypeScript en `functions/`, JS plano sin bundler en `public/assets/`, Vitest (proyectos `unit` / `integration` / `e2e`).

## Global Constraints

- Trabajo en el worktree `D:/Repositorio/web-coparena/.worktrees/torneo-2026`, rama `feature/torneo-2026`. El checkout principal no se toca.
- Copy en español **con tildes**. El deporte se escribe **«volley»**, nunca «vóley».
- Estilos públicos: solo `src/styles/global.css`, con las variables `--sea`, `--lime`, `--coral`, `--sun`, `--dune`… ya definidas en `:root`. Nada de `<style>` con scope en componentes.
- Estilos del panel: `src/styles/admin/torneo.css`, tokens `--adm-*`.
- Todo cambio de frontend es **responsive**; los breakpoints existentes son 900px y 560px, y los valores base de `global.css` son la escala **móvil**.
- Los scripts de `public/assets/` cargan con `<script is:inline>` y **no pueden importar** de `functions/`. Lo que se repita a mano queda cubierto por un test de paridad.
- Migraciones: un fichero por tanda de `ALTER`, porque `ADD COLUMN` no es idempotente y D1 reejecuta el fichero entero si una sentencia falla a mitad.
- Cada tarea acaba con `npm test` en verde. Antes de mezclar en `development`, `npm run verify` completo.
- **Producción no se toca** en ninguna tarea salvo la 7, y allí solo con confirmación explícita del usuario.

## Estructura de ficheros

| Fichero | Responsabilidad |
|---|---|
| `db/migrations/0022_clasificacion_repesca.sql` | *(nuevo)* Las tres columnas. Solo `ALTER`. |
| `functions/_lib/clasificados.ts` | *(nuevo)* Función pura: quién pasa, con qué condición, y en qué orden se siembra. |
| `functions/_lib/clasificacion.ts` | Exponer `valorDeCriterio`, hoy privado, para poder ordenar el bote de repesca. |
| `functions/_lib/torneo-vista.ts` | Llamar a `clasificados.ts` y exponer `clasifica` por fila, `repesca` por fase y `clasifican`/`enRepesca` por grupo. |
| `functions/_lib/torneo.ts` | `sembrarEliminatoria` acepta las semillas ya calculadas. |
| `functions/api/admin/torneo.ts` | Validar y guardar los campos nuevos; `sembrar` consume `clasificados.ts`. |
| `src/pages/admin/torneo.astro` | Campos nuevos en los diálogos de fase y de grupo. |
| `public/assets/admin/torneo-formato.js` | Leer/escribir esos campos y pintar las dos condiciones. |
| `src/styles/admin/torneo.css` | Color de la fila de repesca en el panel. |
| `public/assets/torneo-page.js` | Pintar las dos condiciones y la nota al pie real. |
| `src/styles/global.css` | Color de la fila de repesca en público. |
| `scripts/programar-torneo-2026.mjs` | *(nuevo)* Genera el SQL de horarios a partir de los partidos ya creados. |
| `db/seeds/torneo-2026.sql` | *(nuevo, generado)* Los horarios y pistas, con ids explícitos. |

---

### Task 1: `clasificados.ts` — quién pasa y en qué orden

**Files:**
- Create: `functions/_lib/clasificados.ts`
- Modify: `functions/_lib/clasificacion.ts` (exportar `valorDeCriterio`)
- Test: `test/unit/clasificados.test.ts`

**Interfaces:**
- Consumes: `FilaClasificacion` y `CriterioDesempate`, ya existentes.
- Produces: `calcularClasificados(grupos, repesca, desempates) => { condiciones: Map<number, Condicion>, semillas: Semilla[] }`, con `Condicion = "directo" | "repesca"` y `Semilla = { equipoId: number; nombre: string; grupoId: number; condicion: Condicion }`. Las tareas 2, 3 y 4 dependen de estos nombres exactos.

- [ ] **Step 1: Exportar el cálculo de valor de un criterio**

En `functions/_lib/clasificacion.ts`, la función privada `valor` pasa a ser exportada y renombrada. Sustituye su declaración:

```ts
/** Cuánto vale una fila según un criterio. Siempre "más es mejor". */
export function valorDeCriterio(criterio: CriterioDesempate, fila: Omit<FilaClasificacion, "posicion">): number {
  switch (criterio) {
    case "puntos":
      return fila.puntos;
    case "victorias":
      return fila.ganados;
    case "diferencia_sets":
      return fila.setsAFavor - fila.setsEnContra;
    case "ratio_sets":
      return ratio(fila.setsAFavor, fila.setsEnContra);
    case "diferencia_puntos":
      return fila.puntosAFavor - fila.puntosEnContra;
    case "ratio_puntos":
      return ratio(fila.puntosAFavor, fila.puntosEnContra);
    default:
      // enfrentamiento_directo no se resuelve comparando valores de la tabla
      // general: tiene su propia rama en ordenar().
      return 0;
  }
}
```

Y en los tres sitios donde se llamaba `valor(...)` dentro del fichero (dentro de `agruparPorValor` y dos veces en `agruparPorEmpate`), cambia la llamada a `valorDeCriterio(...)`. No cambia ningún comportamiento: es solo visibilidad.

- [ ] **Step 2: Escribir el test que falla**

Crea `test/unit/clasificados.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { calcularClasificados, type GrupoParaClasificar } from "../../functions/_lib/clasificados";
import type { FilaClasificacion } from "../../functions/_lib/clasificacion";

/** Una fila de clasificación con lo mínimo para ordenar; el resto a cero. */
const fila = (
  posicion: number,
  equipoId: number,
  nombre: string,
  puntos: number,
  extra: Partial<FilaClasificacion> = {}
): FilaClasificacion => ({
  posicion,
  equipoId,
  nombre,
  jugados: 3,
  ganados: 0,
  perdidos: 0,
  setsAFavor: 0,
  setsEnContra: 0,
  puntosAFavor: 0,
  puntosEnContra: 0,
  puntos,
  desempatadoPor: null,
  ...extra
});

const grupo = (
  id: number,
  nombre: string,
  clasifican: number,
  enRepesca: boolean,
  clasificacion: FilaClasificacion[]
): GrupoParaClasificar => ({ id, nombre, clasifican, enRepesca, clasificacion });

const DESEMPATES = ["puntos", "ratio_sets", "ratio_puntos"] as const;

describe("calcularClasificados", () => {
  it("marca como directos a los primeros de cada grupo según su propio cupo", () => {
    const grupos = [
      grupo(1, "A", 2, true, [fila(1, 10, "A1", 9), fila(2, 11, "A2", 6), fila(3, 12, "A3", 3)]),
      grupo(2, "C", 3, false, [fila(1, 20, "C1", 9), fila(2, 21, "C2", 6), fila(3, 22, "C3", 3), fila(4, 23, "C4", 0)])
    ];

    const { condiciones } = calcularClasificados(grupos, 0, DESEMPATES);

    expect(condiciones.get(10)).toBe("directo");
    expect(condiciones.get(11)).toBe("directo");
    expect(condiciones.get(12)).toBeUndefined();
    expect(condiciones.get(22)).toBe("directo");
    expect(condiciones.get(23)).toBeUndefined();
  });

  it("reparte las plazas de repesca solo entre los grupos que entran al bote", () => {
    const grupos = [
      grupo(1, "A", 2, true, [fila(1, 10, "A1", 9), fila(2, 11, "A2", 6), fila(3, 12, "A3", 4)]),
      grupo(2, "B", 2, true, [fila(1, 30, "B1", 9), fila(2, 31, "B2", 6), fila(3, 32, "B3", 1)]),
      grupo(3, "C", 3, false, [fila(1, 20, "C1", 9), fila(2, 21, "C2", 6), fila(3, 22, "C3", 5), fila(4, 23, "C4", 5)])
    ];

    const { condiciones } = calcularClasificados(grupos, 1, DESEMPATES);

    // A3 (4 puntos) gana la repesca a B3 (1 punto).
    expect(condiciones.get(12)).toBe("repesca");
    expect(condiciones.get(32)).toBeUndefined();
    // C4 tiene 5 puntos, más que A3, pero su grupo no entra al bote.
    expect(condiciones.get(23)).toBeUndefined();
  });

  it("ordena las semillas por posición y deja las repescas al final", () => {
    const grupos = [
      grupo(1, "A", 2, true, [fila(1, 10, "A1", 9), fila(2, 11, "A2", 6), fila(3, 12, "A3", 4)]),
      grupo(2, "B", 2, true, [fila(1, 30, "B1", 9), fila(2, 31, "B2", 6), fila(3, 32, "B3", 1)]),
      grupo(3, "C", 3, false, [fila(1, 20, "C1", 9), fila(2, 21, "C2", 6), fila(3, 22, "C3", 5)])
    ];

    const { semillas } = calcularClasificados(grupos, 1, DESEMPATES);

    expect(semillas.map((s) => s.equipoId)).toEqual([10, 30, 20, 11, 31, 21, 22, 12]);
    expect(semillas.at(-1)).toMatchObject({ equipoId: 12, condicion: "repesca" });
  });

  it("los colores y las semillas dicen exactamente lo mismo", () => {
    const grupos = [
      grupo(1, "A", 2, true, [fila(1, 10, "A1", 9), fila(2, 11, "A2", 6), fila(3, 12, "A3", 4)]),
      grupo(2, "B", 2, true, [fila(1, 30, "B1", 9), fila(2, 31, "B2", 6), fila(3, 32, "B3", 1)]),
      grupo(3, "C", 3, false, [fila(1, 20, "C1", 9), fila(2, 21, "C2", 6), fila(3, 22, "C3", 5)])
    ];

    const { condiciones, semillas } = calcularClasificados(grupos, 1, DESEMPATES);

    expect(semillas).toHaveLength(condiciones.size);
    semillas.forEach((s) => expect(condiciones.get(s.equipoId)).toBe(s.condicion));
  });

  it("con un grupo a medio jugar no inventa clasificados de más", () => {
    const grupos = [grupo(1, "A", 2, true, [fila(1, 10, "A1", 3)])];
    const { semillas } = calcularClasificados(grupos, 1, DESEMPATES);
    // Solo hay una fila: una plaza directa y ninguna repesca posible.
    expect(semillas.map((s) => s.equipoId)).toEqual([10]);
  });
});
```

- [ ] **Step 3: Ejecutar el test y verificar que falla**

```bash
npx vitest run --project unit test/unit/clasificados.test.ts
```

Esperado: FAIL, `Failed to resolve import "../../functions/_lib/clasificados"`.

- [ ] **Step 4: Escribir la implementación**

Crea `functions/_lib/clasificados.ts`:

```ts
// Quién pasa de la fase de grupos, y en qué orden entra al cuadro.
//
// Vive aparte de clasificacion.ts porque responde a otra pregunta: aquella
// ordena un grupo, esta mira todos los grupos a la vez y reparte plazas.
//
// Devuelve las dos cosas de una sola pasada —los colores de la tabla y las
// semillas de la siembra— y eso es lo importante. Si el pintado saliera por un
// lado y la siembra por otro, un día la tabla diría que pasa uno y el cuadro
// colocaría a otro, sin forma de saber cuál miente.

import { valorDeCriterio, type FilaClasificacion } from "./clasificacion";
import type { CriterioDesempate } from "./reglas";

export type Condicion = "directo" | "repesca";

export interface GrupoParaClasificar {
  id: number;
  nombre: string;
  /** Plazas directas ya resueltas: las del grupo si las tiene, si no las de la fase. */
  clasifican: number;
  /** Si el siguiente de este grupo entra al bote de la repesca. */
  enRepesca: boolean;
  clasificacion: readonly FilaClasificacion[];
}

export interface Semilla {
  equipoId: number;
  nombre: string;
  grupoId: number;
  condicion: Condicion;
}

export interface Clasificados {
  /** equipoId → condición. Solo lleva a quien pasa. */
  condiciones: Map<number, Condicion>;
  /** En orden de siembra: todos los primeros, luego los segundos… y las repescas al final. */
  semillas: Semilla[];
}

export function calcularClasificados(
  grupos: readonly GrupoParaClasificar[],
  repesca: number,
  desempates: readonly CriterioDesempate[]
): Clasificados {
  const condiciones = new Map<number, Condicion>();
  const semillas: Semilla[] = [];

  /*
   * Se siembra por posición y no grupo a grupo: primero todos los primeros,
   * luego todos los segundos. Así el emparejamiento 1.º contra último cruza
   * cabezas de serie con colistas, que es lo que hace que un cuadro tenga
   * sentido. El tope es el cupo más alto, porque los grupos pueden no tener el
   * mismo número de plazas directas.
   */
  const maximo = grupos.reduce((tope, g) => Math.max(tope, g.clasifican), 0);
  for (let posicion = 1; posicion <= maximo; posicion += 1) {
    for (const grupo of grupos) {
      if (posicion > grupo.clasifican) continue;
      const fila = grupo.clasificacion.find((f) => f.posicion === posicion);
      if (!fila) continue;
      condiciones.set(fila.equipoId, "directo");
      semillas.push({ equipoId: fila.equipoId, nombre: fila.nombre, grupoId: grupo.id, condicion: "directo" });
    }
  }

  /*
   * El bote: el siguiente de cada grupo que participe. `enfrentamiento_directo`
   * se salta porque entre equipos de grupos distintos no hay partidos que mirar
   * — daría siempre empate y solo estorbaría.
   */
  const criterios = desempates.filter((c) => c !== "enfrentamiento_directo");
  const bote = grupos
    .filter((grupo) => grupo.enRepesca)
    .map((grupo) => ({
      grupo,
      fila: grupo.clasificacion.find((f) => f.posicion === grupo.clasifican + 1)
    }))
    .filter((candidato): candidato is { grupo: GrupoParaClasificar; fila: FilaClasificacion } =>
      candidato.fila !== undefined
    );

  bote.sort((a, b) => {
    for (const criterio of criterios) {
      const diferencia = valorDeCriterio(criterio, b.fila) - valorDeCriterio(criterio, a.fila);
      if (diferencia !== 0) return diferencia;
    }
    // Empate perfecto: por nombre, para que al menos sea estable y reproducible.
    return a.fila.nombre.localeCompare(b.fila.nombre, "es");
  });

  bote.slice(0, Math.max(0, repesca)).forEach(({ grupo, fila }) => {
    condiciones.set(fila.equipoId, "repesca");
    semillas.push({ equipoId: fila.equipoId, nombre: fila.nombre, grupoId: grupo.id, condicion: "repesca" });
  });

  return { condiciones, semillas };
}
```

- [ ] **Step 5: Ejecutar los tests y verificar que pasan**

```bash
npx vitest run --project unit test/unit/clasificados.test.ts test/unit/clasificacion.test.ts
```

Esperado: PASS en los dos ficheros. `clasificacion.test.ts` tiene que seguir verde: el cambio del paso 1 es solo de visibilidad.

- [ ] **Step 6: Commit**

```bash
git add functions/_lib/clasificados.ts functions/_lib/clasificacion.ts test/unit/clasificados.test.ts
git commit -m "feat: clasificados.ts decide quien pasa y en que orden se siembra"
```

---

### Task 2: La migración y las columnas nuevas

**Files:**
- Create: `db/migrations/0022_clasificacion_repesca.sql`
- Modify: `functions/_lib/torneo.ts` (interfaces `FaseRow` y `GrupoRow`)
- Test: se verifica aplicando la migración; sin test propio (una migración de solo `ALTER` la prueban las tareas siguientes).

**Interfaces:**
- Produces: `FaseRow.repesca: number`, `GrupoRow.clasifican: number | null`, `GrupoRow.en_repesca: number`. Las tareas 3 y 4 leen estos nombres.

- [ ] **Step 1: Escribir la migración**

Crea `db/migrations/0022_clasificacion_repesca.sql`:

```sql
-- Migration number: 0022 	 plazas directas por grupo y repesca por fase
-- Solo ALTER, y por eso va en su propio fichero: ADD COLUMN no es idempotente y
-- D1 reejecuta el fichero entero si una sentencia falla a mitad.
--
-- `torneo_fases.clasifican` solo sabe decir "los N primeros de CADA grupo", y eso
-- no siempre es la regla. Con grupos de tamanos distintos, el tercero de un grupo
-- de cinco puede pasar directo mientras los terceros de los grupos de cuatro se
-- juegan una sola plaza entre ellos.
--
-- `clasifican` NULL en el grupo significa "hereda de la fase", igual que `reglas`
-- y por el mismo motivo: distinguir "hereda" de "coincide por casualidad" importa
-- al editar.
--
-- `en_repesca` por defecto 1 para que nada existente cambie de comportamiento:
-- sin plazas de repesca en la fase (repesca = 0), da igual quien este en el bote.

ALTER TABLE torneo_grupos ADD COLUMN clasifican INTEGER;
ALTER TABLE torneo_grupos ADD COLUMN en_repesca INTEGER NOT NULL DEFAULT 1;
ALTER TABLE torneo_fases ADD COLUMN repesca INTEGER NOT NULL DEFAULT 0;
```

- [ ] **Step 2: Aplicar la migración en local y comprobar que las columnas existen**

```bash
npx wrangler d1 migrations apply DB --local
```

```bash
npx wrangler d1 execute DB --local --command "SELECT clasifican, en_repesca FROM torneo_grupos LIMIT 1; SELECT repesca FROM torneo_fases LIMIT 1"
```

Esperado: las dos consultas ejecutan sin error de columna desconocida (pueden devolver cero filas).

- [ ] **Step 3: Añadir las columnas a las interfaces**

En `functions/_lib/torneo.ts`, dentro de `export interface FaseRow`, después de `clasifican: number;`:

```ts
  repesca: number;
```

Y dentro de `export interface GrupoRow`, después de `reglas: string | null;`:

```ts
  clasifican: number | null;
  en_repesca: number;
```

- [ ] **Step 4: Comprobar que sigue compilando y en verde**

```bash
npm test
```

Esperado: PASS. Los tests de integración aplican las migraciones solos, así que la 0022 entra sin tocar nada más.

- [ ] **Step 5: Commit**

```bash
git add db/migrations/0022_clasificacion_repesca.sql functions/_lib/torneo.ts
git commit -m "feat: migracion 0022, plazas directas por grupo y repesca por fase"
```

---

### Task 3: `torneo-vista.ts` expone la condición de cada fila

**Files:**
- Modify: `functions/_lib/torneo-vista.ts:81-116`
- Test: `test/integration/torneo-publico.test.ts`

**Interfaces:**
- Consumes: `calcularClasificados` de la tarea 1; `FaseRow.repesca`, `GrupoRow.clasifican`, `GrupoRow.en_repesca` de la tarea 2.
- Produces: en la salida de `cargarTorneo`, cada fase lleva `repesca: number`, cada grupo lleva `clasifican: number` (ya resuelto) y `enRepesca: boolean`, y cada fila de `clasificacion` lleva `clasifica: "directo" | "repesca" | null`. Las tareas 4, 5 y 6 leen estos nombres.

- [ ] **Step 1: Escribir el test que falla**

Añade a `test/integration/torneo-publico.test.ts` (usa los imports y helpers que el fichero ya tiene; si `crearGrupo`, `asignarEquipoAGrupo` o `crearPartido` no están importados, añádelos a la línea de import desde `../helpers/db`):

```ts
it("marca cada fila con si pasa directo, por repesca o no pasa", async () => {
  const fase = await crearFase({ tipo: "grupos", clasifican: 1 });
  await env.DB.prepare("UPDATE torneo_fases SET repesca = 1 WHERE id = ?1").bind(fase.id).run();

  const grupoA = await crearGrupo(fase.id, { nombre: "A", orden: 0 });
  const grupoB = await crearGrupo(fase.id, { nombre: "B", orden: 1 });

  const a1 = await crearEquipo({ nombre: "A uno" });
  const a2 = await crearEquipo({ nombre: "A dos" });
  const b1 = await crearEquipo({ nombre: "B uno" });
  const b2 = await crearEquipo({ nombre: "B dos" });

  await asignarEquipoAGrupo(grupoA, fase.id, a1.id);
  await asignarEquipoAGrupo(grupoA, fase.id, a2.id);
  await asignarEquipoAGrupo(grupoB, fase.id, b1.id);
  await asignarEquipoAGrupo(grupoB, fase.id, b2.id);

  // A uno gana 2-0; B uno gana 2-0 pero con menos puntos, así que su segundo
  // no es el mejor segundo.
  await crearPartido({
    faseId: fase.id, grupoId: grupoA, equipoA: a1, equipoB: a2,
    status: "finished", winner: "A", setsA: 2, setsB: 0, puntosA: 42, puntosB: 20
  });
  await crearPartido({
    faseId: fase.id, grupoId: grupoB, equipoA: b1, equipoB: b2,
    status: "finished", winner: "A", setsA: 2, setsB: 0, puntosA: 42, puntosB: 10
  });

  const respuesta = await onRequestGet(ctx(await peticion("/api/torneo"), env) as never);
  const datos = (await respuesta.json()) as {
    fases: { repesca: number; grupos: { nombre: string; clasifican: number; enRepesca: boolean;
      clasificacion: { nombre: string; clasifica: string | null }[] }[] }[];
  };

  const salida = datos.fases[0]!;
  expect(salida.repesca).toBe(1);

  const condicion = (nombre: string) =>
    salida.grupos.flatMap((g) => g.clasificacion).find((f) => f.nombre === nombre)?.clasifica;

  expect(condicion("A uno")).toBe("directo");
  expect(condicion("B uno")).toBe("directo");
  // De los dos segundos, pasa por repesca el que encajó menos: A dos.
  expect(condicion("A dos")).toBe("repesca");
  expect(condicion("B dos")).toBeNull();
});
```

- [ ] **Step 2: Ejecutar el test y verificar que falla**

```bash
npx vitest run --project integration test/integration/torneo-publico.test.ts -t "marca cada fila"
```

Esperado: FAIL, `expected undefined to be 1` sobre `salida.repesca`.

- [ ] **Step 3: Implementar en `torneo-vista.ts`**

Añade el import al principio del fichero, junto a los que ya hay:

```ts
import { calcularClasificados, type GrupoParaClasificar } from "./clasificados";
```

Sustituye entero el bloque `const fasesSalida = fases.results.map((fase) => { … });` (líneas 81-116) por:

```ts
  const fasesSalida = fases.results.map((fase) => {
    const gruposDeFase = grupos.results.filter((g) => g.fase_id === fase.id);
    const partidosDeFase = partidos.results.filter((p) => p.fase_id === fase.id);
    const reglasDeFase = normalizarReglas(fase.reglas);

    // Primero cada grupo por su cuenta; el reparto de plazas mira a todos a la vez.
    const calculados = gruposDeFase.map((grupo) => {
      const equiposDelGrupo = asignados.results
        .filter((a) => a.grupo_id === grupo.id)
        .map((a) => ({ id: a.equipo_id, nombre: a.nombre }));
      const reglas = reglasEfectivas(fase, grupo);

      return {
        grupo,
        equiposDelGrupo,
        reglas,
        // El cupo del grupo si lo tiene; si no, el de la fase.
        clasifican: grupo.clasifican ?? fase.clasifican,
        enRepesca: grupo.en_repesca !== 0,
        clasificacion: clasificacionDeGrupo(
          equiposDelGrupo,
          partidosDeFase.filter((p) => p.grupo_id === grupo.id).map(aClasificable),
          reglas.clasificacion
        )
      };
    });

    const paraClasificar: GrupoParaClasificar[] = calculados.map((c) => ({
      id: c.grupo.id,
      nombre: c.grupo.nombre,
      clasifican: c.clasifican,
      enRepesca: c.enRepesca,
      clasificacion: c.clasificacion
    }));
    const { condiciones } = calcularClasificados(paraClasificar, fase.repesca, reglasDeFase.clasificacion.desempates);

    return {
      id: fase.id,
      clave: fase.clave,
      nombre: fase.nombre,
      tipo: fase.tipo,
      orden: fase.orden,
      clasifican: fase.clasifican,
      repesca: fase.repesca,
      reglas: reglasDeFase,
      grupos: calculados.map((c) => ({
        id: c.grupo.id,
        nombre: c.grupo.nombre,
        orden: c.grupo.orden,
        /** null si hereda; distinguirlo de «iguales por casualidad» importa al editar. */
        reglasPropias: c.grupo.reglas === null ? null : normalizarReglas(c.grupo.reglas),
        reglas: c.reglas,
        /** Ya resuelto: el del grupo si lo tiene, si no el de la fase. */
        clasifican: c.clasifican,
        /** null en el grupo significa que hereda, y al editar hay que saberlo. */
        clasificanPropio: c.grupo.clasifican,
        enRepesca: c.enRepesca,
        equipos: c.equiposDelGrupo,
        clasificacion: c.clasificacion.map((fila) => ({
          ...fila,
          clasifica: condiciones.get(fila.equipoId) ?? null
        }))
      })),
      partidos: partidosDeFase.map(mapPartido)
    };
  });
```

- [ ] **Step 4: Ejecutar los tests y verificar que pasan**

```bash
npx vitest run --project integration test/integration/torneo-publico.test.ts test/integration/torneo-admin.test.ts
```

Esperado: PASS en los dos.

- [ ] **Step 5: Commit**

```bash
git add functions/_lib/torneo-vista.ts test/integration/torneo-publico.test.ts
git commit -m "feat: cada fila de clasificacion dice si pasa directo o por repesca"
```

- [ ] **Step 6: Fijar por test la trampa del partido a un set**

Un partido a un set siempre cuenta como «resuelto en el set decisivo», porque `setsMaximos({sets: 1})` vale 1 y `aClasificable` compara `sets_a + sets_b >= setsMaximos`. El grupo C del torneo 2026 depende de eso: lleva un bloque `clasificacion` propio precisamente para compensarlo. Sin un test, alguien «arregla» `setDecisivo` algún día y el grupo C empieza a puntuar mal en silencio.

Añade a `test/unit/clasificacion.test.ts` (añade los imports que falten al principio del fichero):

```ts
import { aClasificable, type PartidoVistaRow } from "../../functions/_lib/torneo-vista";
import { normalizarReglas } from "../../functions/_lib/reglas";

describe("un partido a un solo set", () => {
  const partidoAUnSet = (reglas: unknown): PartidoVistaRow => ({
    id: "p1",
    ronda: "C · jornada 1",
    fase_id: 1,
    grupo_id: 1,
    ronda_orden: null,
    posicion: null,
    equipo_a_id: 10,
    equipo_b_id: 20,
    equipo_a_nombre: "Uno",
    equipo_b_nombre: "Dos",
    scheduled_at: null,
    pista: null,
    status: "finished",
    sets_a: 1,
    sets_b: 0,
    points_a: 21,
    points_b: 15,
    set_history: JSON.stringify([{ a: 21, b: 15 }]),
    winner: "A",
    reglas: JSON.stringify(reglas),
    siguiente_partido_id: null,
    perdedor_partido_id: null
  });

  const equipos = [
    { id: 10, nombre: "Uno" },
    { id: 20, nombre: "Dos" }
  ];

  const SIN_OVERRIDE = { partido: { sets: 1, puntosPorSet: 21, puntosSetDecisivo: 15, diferencia: 2 } };
  const GRUPO_C = {
    ...SIN_OVERRIDE,
    clasificacion: {
      puntosVictoria: 3,
      puntosDerrota: 0,
      puntosVictoriaAjustada: 3,
      puntosDerrotaAjustada: 0,
      desempates: ["puntos", "ratio_puntos"]
    }
  };

  const puntosDe = (reglas: unknown) => {
    const tabla = clasificacionDeGrupo(
      equipos,
      [aClasificable(partidoAUnSet(reglas))],
      normalizarReglas(reglas).clasificacion
    );
    return {
      ganador: tabla.find((f) => f.equipoId === 10)!.puntos,
      perdedor: tabla.find((f) => f.equipoId === 20)!.puntos
    };
  };

  it("siempre cuenta como resuelto en el set decisivo", () => {
    expect(aClasificable(partidoAUnSet(SIN_OVERRIDE)).setDecisivo).toBe(true);
  });

  it("por eso, sin bloque de clasificación propio, puntúa 2-1 en vez de 3-0", () => {
    expect(puntosDe(SIN_OVERRIDE)).toEqual({ ganador: 2, perdedor: 1 });
  });

  it("con el bloque que lleva el grupo C, vuelve a puntuar 3-0", () => {
    expect(puntosDe(GRUPO_C)).toEqual({ ganador: 3, perdedor: 0 });
  });
});
```

- [ ] **Step 7: Ejecutar el test y verificar que pasa a la primera**

```bash
npx vitest run --project unit test/unit/clasificacion.test.ts
```

Esperado: PASS. Este es un test de caracterización: documenta lo que el código **ya** hace, así que no tiene fase roja. Si alguno de los tres falla, el comportamiento no es el que el diseño asumió y hay que parar y avisar antes de seguir.

- [ ] **Step 8: Commit**

```bash
git add test/unit/clasificacion.test.ts
git commit -m "test: fijar que un partido a un set siempre es 'decisivo'"
```

---

### Task 4: El endpoint guarda los campos nuevos y siembra desde `clasificados.ts`

**Files:**
- Modify: `functions/api/admin/torneo.ts` (`validarFase`, `crearFase`, `editarFase`, `crearGrupo`, `editarGrupo`, `accionSembrar`)
- Modify: `functions/_lib/torneo.ts` (`sembrarEliminatoria`)
- Test: `test/integration/torneo-admin.test.ts`

**Interfaces:**
- Consumes: `calcularClasificados` (tarea 1), la salida de `cargarTorneo` (tarea 3).
- Produces: `POST/PATCH /api/admin/torneo?accion=fase` acepta `repesca` (0-8); `?accion=grupo` acepta `clasifican` (número o `null`) y `enRepesca` (booleano). La tarea 5 envía estos nombres.

- [ ] **Step 1: Escribir el test que falla**

Añade a `test/integration/torneo-admin.test.ts`:

```ts
it("guarda la repesca de la fase y el cupo propio de un grupo", async () => {
  const admin = await crearAdmin();

  const creada = await onRequestPost(
    ctx(await peticion("/api/admin/torneo?accion=fase", {
      method: "POST", usuario: admin,
      body: { clave: "grupos", nombre: "Fase de grupos", tipo: "grupos", clasifican: 2, repesca: 1 }
    }), env) as never
  );
  expect(creada.status).toBe(201);
  const trasCrear = (await creada.json()) as { fases: { id: number; clasifican: number; repesca: number }[] };
  const fase = trasCrear.fases[0]!;
  expect(fase.repesca).toBe(1);

  const grupo = await onRequestPost(
    ctx(await peticion(`/api/admin/torneo?accion=grupo&fase=${fase.id}`, {
      method: "POST", usuario: admin,
      body: { nombre: "C", orden: 2, clasifican: 3, enRepesca: false }
    }), env) as never
  );
  expect(grupo.status).toBe(201);

  const datos = (await grupo.json()) as {
    fases: { grupos: { nombre: string; clasifican: number; clasificanPropio: number | null; enRepesca: boolean }[] }[];
  };
  const c = datos.fases[0]!.grupos.find((g) => g.nombre === "C")!;
  expect(c.clasifican).toBe(3);
  expect(c.clasificanPropio).toBe(3);
  expect(c.enRepesca).toBe(false);
});

it("un grupo sin cupo propio hereda el de la fase", async () => {
  const admin = await crearAdmin();
  const fase = await crearFase({ tipo: "grupos", clasifican: 2 });
  await onRequestPost(
    ctx(await peticion(`/api/admin/torneo?accion=grupo&fase=${fase.id}`, {
      method: "POST", usuario: admin, body: { nombre: "A", orden: 0 }
    }), env) as never
  );

  const respuesta = await onRequestGet(ctx(await peticion("/api/admin/torneo", { usuario: admin }), env) as never);
  const datos = (await respuesta.json()) as {
    fases: { grupos: { nombre: string; clasifican: number; clasificanPropio: number | null }[] }[];
  };
  const a = datos.fases[0]!.grupos.find((g) => g.nombre === "A")!;
  expect(a.clasifican).toBe(2);
  expect(a.clasificanPropio).toBeNull();
});
```

- [ ] **Step 2: Ejecutar los tests y verificar que fallan**

```bash
npx vitest run --project integration test/integration/torneo-admin.test.ts -t "repesca de la fase"
```

Esperado: FAIL, `expected undefined to be 1` sobre `fase.repesca`.

- [ ] **Step 3: Implementar la validación y el guardado**

En `functions/api/admin/torneo.ts`:

En la interfaz `FaseValidada`, añade tras `clasifican: number;`:

```ts
  repesca: number;
```

En `validarFase`, después del bloque que valida `clasifican`, añade:

```ts
  const repesca = Number(body.repesca ?? 0);
  if (!Number.isInteger(repesca) || repesca < 0 || repesca > 8) {
    campos.repesca = "Tiene que estar entre 0 y 8.";
  }
```

y en el objeto que devuelve al final, tras `clasifican,`:

```ts
    repesca,
```

En `crearFase`, cambia el `INSERT` y su `bind`:

```ts
  await db
    .prepare(
      `INSERT INTO torneo_fases (edicion_id, clave, nombre, tipo, orden, reglas, clasifican, repesca)
       VALUES (?1, ?2, ?3, ?4, ?5, ?6, ?7, ?8)`
    )
    .bind(edicion.id, datos.clave, datos.nombre, datos.tipo, datos.orden, JSON.stringify(datos.reglas), datos.clasifican, datos.repesca)
    .run();
```

En `editarFase`, cambia el `UPDATE` y su `bind`:

```ts
  await db
    .prepare(
      `UPDATE torneo_fases SET nombre = ?1, tipo = ?2, orden = ?3, reglas = ?4, clasifican = ?5,
              repesca = ?6, updated_at = datetime('now')
       WHERE id = ?7`
    )
    .bind(datos.nombre, datos.tipo, datos.orden, JSON.stringify(datos.reglas), datos.clasifican, datos.repesca, id)
    .run();
```

Añade este helper junto a `leerReglasOpcionales`, al final del fichero:

```ts
/**
 * El cupo propio de un grupo. `null` (o ausente) significa «hereda de la fase»,
 * que no es lo mismo que 0: 0 sería «de aquí no pasa nadie».
 */
function leerCupoOpcional(body: Record<string, unknown>): number | null | Response {
  if (body.clasifican === undefined || body.clasifican === null || body.clasifican === "") return null;
  const n = Number(body.clasifican);
  if (!Number.isInteger(n) || n < 0 || n > 8) {
    return jsonAdmin({ error: "Revisa los campos marcados.", campos: { clasifican: "Tiene que estar entre 0 y 8." } }, 400);
  }
  return n;
}
```

En `crearGrupo`, después de `const reglas = leerReglasOpcionales(body);` y su guarda, añade:

```ts
  const cupo = leerCupoOpcional(body);
  if (cupo instanceof Response) return cupo;
  const enRepesca = body.enRepesca === false ? 0 : 1;
```

y cambia el `INSERT`:

```ts
  await db
    .prepare("INSERT INTO torneo_grupos (fase_id, nombre, orden, reglas, clasifican, en_repesca) VALUES (?1, ?2, ?3, ?4, ?5, ?6)")
    .bind(faseId, nombre, Number(body.orden) || 0, reglas, cupo, enRepesca)
    .run();
```

En `editarGrupo`, igual: añade las mismas dos líneas tras la guarda de `reglas` y cambia el `UPDATE`:

```ts
  await db
    .prepare(
      `UPDATE torneo_grupos SET nombre = ?1, orden = ?2, reglas = ?3, clasifican = ?4, en_repesca = ?5,
              updated_at = datetime('now')
       WHERE id = ?6`
    )
    .bind(nombre, Number(body.orden) || 0, reglas, cupo, enRepesca, id)
    .run();
```

- [ ] **Step 4: Ejecutar los tests y verificar que pasan**

```bash
npx vitest run --project integration test/integration/torneo-admin.test.ts
```

Esperado: PASS todo el fichero.

- [ ] **Step 5: Commit**

```bash
git add functions/api/admin/torneo.ts test/integration/torneo-admin.test.ts
git commit -m "feat: el panel guarda repesca por fase y cupo propio por grupo"
```

- [ ] **Step 6: Escribir el test de la siembra**

Añade a `test/integration/torneo-admin.test.ts`:

```ts
it("siembra exactamente a los que la clasificacion pinta como clasificados", async () => {
  const admin = await crearAdmin();

  const grupos = await crearFase({ clave: "grupos", tipo: "grupos", clasifican: 1 });
  await env.DB.prepare("UPDATE torneo_fases SET repesca = 1 WHERE id = ?1").bind(grupos.id).run();
  const gA = await crearGrupo(grupos.id, { nombre: "A", orden: 0 });
  const gB = await crearGrupo(grupos.id, { nombre: "B", orden: 1 });

  const a1 = await crearEquipo({ nombre: "A uno" });
  const a2 = await crearEquipo({ nombre: "A dos" });
  const b1 = await crearEquipo({ nombre: "B uno" });
  const b2 = await crearEquipo({ nombre: "B dos" });
  await asignarEquipoAGrupo(gA, grupos.id, a1.id);
  await asignarEquipoAGrupo(gA, grupos.id, a2.id);
  await asignarEquipoAGrupo(gB, grupos.id, b1.id);
  await asignarEquipoAGrupo(gB, grupos.id, b2.id);

  await crearPartido({ faseId: grupos.id, grupoId: gA, equipoA: a1, equipoB: a2,
    status: "finished", winner: "A", setsA: 2, setsB: 0, puntosA: 42, puntosB: 20 });
  await crearPartido({ faseId: grupos.id, grupoId: gB, equipoA: b1, equipoB: b2,
    status: "finished", winner: "A", setsA: 2, setsB: 0, puntosA: 42, puntosB: 10 });

  const cuadro = await crearFase({ clave: "cuadro", tipo: "eliminatoria", orden: 1 });
  await onRequestPost(
    ctx(await peticion(`/api/admin/torneo?accion=generar-cuadro&fase=${cuadro.id}`, {
      method: "POST", usuario: admin, body: { tamano: 2 }
    }), env) as never
  );

  const sembrada = await onRequestPost(
    ctx(await peticion(`/api/admin/torneo?accion=sembrar&fase=${cuadro.id}`, {
      method: "POST", usuario: admin, body: { desdeFase: grupos.id }
    }), env) as never
  );
  expect(sembrada.status).toBe(200);

  const { results } = await env.DB
    .prepare("SELECT equipo_a_nombre, equipo_b_nombre FROM partidos WHERE fase_id = ?1")
    .bind(cuadro.id)
    .all<{ equipo_a_nombre: string; equipo_b_nombre: string }>();

  // Pasan los dos primeros (directos) y el mejor segundo (repesca): con un cuadro
  // de 2, entran el primer sembrado y el último.
  const puestos = [results[0]!.equipo_a_nombre, results[0]!.equipo_b_nombre];
  expect(puestos).toContain("A uno");
  expect(puestos).toContain("A dos");
});
```

- [ ] **Step 7: Ejecutar el test y verificar que falla**

```bash
npx vitest run --project integration test/integration/torneo-admin.test.ts -t "siembra exactamente"
```

Esperado: FAIL — `accionSembrar` todavía recorre posiciones a mano y no conoce la repesca.

- [ ] **Step 8: Reescribir `accionSembrar`**

En `functions/api/admin/torneo.ts`, sustituye el cuerpo de `accionSembrar` desde el comentario `/* Se siembra por posición…` hasta el `await sembrarEliminatoria(...)` por:

```ts
  const paraClasificar = origen.grupos.map((grupo) => ({
    id: grupo.id,
    nombre: grupo.nombre,
    clasifican: grupo.clasifican,
    enRepesca: grupo.enRepesca,
    clasificacion: grupo.clasificacion
  }));

  /*
   * Las semillas salen de la MISMA función que pinta los colores de la tabla. Si
   * se recalculasen aquí, un día la tabla diría que pasa uno y el cuadro
   * colocaría a otro.
   */
  const { semillas } = calcularClasificados(paraClasificar, origen.repesca, origen.reglas.clasificacion.desempates);

  if (semillas.length === 0) return jsonAdmin({ error: "No hay ningún equipo clasificado todavía." }, 409);

  await sembrarEliminatoria(db, faseId, semillas);
  return await respuesta(db);
```

Elimina la guarda `if (desde.clasifican < 1)` y sustitúyela por una que también admita la repesca:

```ts
  if (desde.clasifican < 1 && desde.repesca < 1) {
    return jsonAdmin({ error: "La fase de origen no dice cuántos equipos clasifican." }, 409);
  }
```

Añade el import al principio del fichero:

```ts
import { calcularClasificados } from "../../_lib/clasificados";
```

En `functions/_lib/torneo.ts`, la interfaz `Semilla` local ya no hace falta: sustitúyela por un re-export para que no haya dos definiciones del mismo concepto:

```ts
export type { Semilla } from "./clasificados";
```

y borra el bloque `export interface Semilla { equipoId: number; nombre: string; }`. `sembrarEliminatoria` no cambia: solo lee `equipoId` y `nombre`, que la nueva `Semilla` también trae.

- [ ] **Step 9: Ejecutar los tests y verificar que pasan**

```bash
npm test
```

Esperado: PASS toda la suite `unit` + `integration`.

- [ ] **Step 10: Commit**

```bash
git add functions/api/admin/torneo.ts functions/_lib/torneo.ts test/integration/torneo-admin.test.ts
git commit -m "feat: la siembra del cuadro sale de la misma funcion que pinta los colores"
```

---

### Task 5: El panel — campos nuevos y color de repesca

**Files:**
- Modify: `src/pages/admin/torneo.astro:179-184` (diálogo de fase) y `:273-277` (diálogo de grupo)
- Modify: `public/assets/admin/torneo-formato.js`
- Modify: `src/styles/admin/torneo.css`

**Interfaces:**
- Consumes: `fase.repesca`, `grupo.clasifican`, `grupo.clasificanPropio`, `grupo.enRepesca` y `fila.clasifica` de las tareas 3 y 4.

- [ ] **Step 1: Añadir el campo de repesca al diálogo de fase**

En `src/pages/admin/torneo.astro`, justo después del `<div class="admin-field" data-fase-solo-grupos>` que contiene `fase-clasifican` (cierra en la línea 183), añade:

```astro
              <div class="admin-field" data-fase-solo-grupos>
                <label for="fase-repesca">Plazas de repesca</label>
                <input id="fase-repesca" class="admin-input" type="number" min="0" max="8" data-fase-field="repesca" />
                <p class="admin-field-hint">Plazas extra que se disputan entre grupos los que quedan justo fuera del cupo.</p>
                <p class="admin-field-error" data-fase-error="repesca" hidden></p>
              </div>
```

- [ ] **Step 2: Añadir los campos al diálogo de grupo**

En el mismo fichero, después del `<div class="admin-field">` que contiene `grupo-orden` (cierra en la línea 276), añade:

```astro
              <div class="admin-field">
                <label for="grupo-clasifican">Clasifican de este grupo</label>
                <input id="grupo-clasifican" class="admin-input" type="number" min="0" max="8" placeholder="Hereda de la fase" data-grupo-field="clasifican" />
                <p class="admin-field-hint">Vacío = el cupo de la fase. Un grupo más grande puede dar más plazas.</p>
                <p class="admin-field-error" data-grupo-error="clasifican" hidden></p>
              </div>
            </div>

            <label class="admin-check">
              <input type="checkbox" data-grupo-field="enRepesca" />
              El siguiente de este grupo entra en la repesca
            </label>
            <div class="admin-form-grid" hidden>
```

Nota: los dos bloques extra de `</div>` y `<div class="admin-form-grid" hidden>` mantienen el `admin-check` **fuera** de la rejilla de dos columnas, donde se vería descolocado. El `<div>` vacío final lo cierra el `</div>` que ya existía.

- [ ] **Step 3: Leer y escribir los campos nuevos en el script**

En `public/assets/admin/torneo-formato.js`:

En `CAMPOS_ERROR_FASE`, añade `"repesca"` después de `"clasifican"`.

En `abrirFase`, después de `campoFase("clasifican").value = String(fase?.clasifican ?? 2);`:

```js
    campoFase("repesca").value = String(fase?.repesca ?? 0);
```

En `leerFase`, en el objeto que devuelve, después de `clasifican: numero("clasifican") || 0,`:

```js
      repesca: numero("repesca") || 0,
```

En `abrirGrupo`, después de `campoGrupo("propias").checked = Boolean(grupo?.reglasPropias);`:

```js
    // Vacío significa «hereda de la fase», que no es lo mismo que 0.
    campoGrupo("clasifican").value = grupo?.clasificanPropio == null ? "" : String(grupo.clasificanPropio);
    campoGrupo("enRepesca").checked = grupo ? grupo.enRepesca !== false : true;
```

En el listener de `[data-grupo-guardar]`, dentro del objeto `datos`, después de `orden: numero("orden") || 0,`:

```js
      // Cadena vacía = hereda; el endpoint distingue null de 0.
      clasifican: campoGrupo("clasifican").value.trim() === "" ? null : numero("clasifican"),
      enRepesca: campoGrupo("enRepesca").checked,
```

- [ ] **Step 4: Pintar las dos condiciones en la tabla del panel**

En la misma función `tablaClasificacion(grupo)`, dentro del `forEach` y justo antes de `tbody.append(tr);`, añade:

```js
      // Quién pasa es la lectura principal de la tabla; y pasar por repesca no
      // es lo mismo que pasar directo, así que no comparten color.
      if (fila.clasifica) tr.classList.add(`is-${fila.clasifica}`);
```

Y en `resumenDeReglas`, sustituye la última línea de la rama de grupos para que diga también la repesca:

```js
    const repescaTexto = clasifican === 0 ? "" : ` · clasifican ${clasifican} por grupo`;
    return `${formato} · ${ventaja} · victoria ${c.puntosVictoria} (${c.puntosVictoriaAjustada} en el decisivo)${repescaTexto}`;
```

En `tarjetaFase`, cambia la llamada para que pueda añadir la repesca detrás:

```js
    const resumen = resumenDeReglas(fase.reglas, fase.tipo, fase.clasifican);
    const conRepesca = fase.repesca > 0 ? `${resumen} + ${fase.repesca} de repesca` : resumen;
    caja.append(cabecera, el("p", "admin-hint", conRepesca));
```

Y en `tarjetaGrupo`, tras la línea que añade la etiqueta de reglas propias, añade otra para el cupo propio:

```js
    if (grupo.clasificanPropio != null) titulo.append(etiqueta(`Pasan ${grupo.clasificanPropio}`, "info"));
    if (grupo.enRepesca === false) titulo.append(etiqueta("Sin repesca", "warn"));
```

- [ ] **Step 5: Añadir el color al CSS del panel**

Al final de `src/styles/admin/torneo.css`:

```css
/*
 * Dos condiciones, dos colores. Pasar directo y pasar por repesca no son lo
 * mismo, y la tabla es donde se lee quién está dentro y quién depende de otro
 * grupo.
 */
.torneo-clasificacion tbody tr.is-directo {
  background: color-mix(in srgb, var(--adm-ok) 14%, transparent);
}

.torneo-clasificacion tbody tr.is-directo td:first-child {
  box-shadow: inset 3px 0 0 var(--adm-ok);
  font-weight: 700;
}

.torneo-clasificacion tbody tr.is-repesca {
  background: color-mix(in srgb, var(--adm-warn) 12%, transparent);
}

.torneo-clasificacion tbody tr.is-repesca td:first-child {
  box-shadow: inset 3px 0 0 var(--adm-warn);
  font-weight: 700;
}
```

Los dos tokens existen y están verificados: `--adm-ok: #2f6b34` y `--adm-warn: #8a6a12` (`src/styles/admin/tokens.css:41-42`).

- [ ] **Step 6: Comprobar que compila y sigue en verde**

```bash
npm run build && npm test
```

Esperado: build sin errores de `astro check`, y suite en verde.

- [ ] **Step 7: Commit**

```bash
git add src/pages/admin/torneo.astro public/assets/admin/torneo-formato.js src/styles/admin/torneo.css
git commit -m "feat: el panel edita la repesca y pinta las dos condiciones"
```

---

### Task 6: La página pública — dos colores y la nota real

**Files:**
- Modify: `public/assets/torneo-page.js:128,145-189`
- Modify: `src/styles/global.css:4928-4936`

**Interfaces:**
- Consumes: `fila.clasifica`, `grupo.clasifican`, `fase.repesca` de la tarea 3.

- [ ] **Step 1: Pasar el grupo entero y la fase a la tabla**

En `public/assets/torneo-page.js`, cambia la llamada de la línea 128:

```js
    bloque.append(tablaClasificacion(grupo, fase));
```

- [ ] **Step 2: Reescribir `tablaClasificacion`**

Sustituye la firma y las dos partes que usaban `clasifican`:

```js
  function tablaClasificacion(grupo, fase) {
```

Dentro del `forEach` de filas, sustituye la línea `if (clasifican > 0 && fila.posicion <= clasifican) tr.classList.add("is-clasificado");` por:

```js
      // Pasar directo y pasar por repesca no son lo mismo: colores distintos.
      if (fila.clasifica) tr.classList.add(`is-${fila.clasifica}`);
```

Y sustituye el bloque final `if (clasifican > 0) { … }` por:

```js
    const nota = notaDeClasificacion(grupo, fase);
    if (nota) caja.append(el("p", "torneo-nota", nota));
    return caja;
  }

  /*
   * La nota describe la regla real, que ya no es «pasan los N primeros»: cada
   * grupo puede dar un número distinto de plazas y encima puede haber repesca.
   */
  function notaDeClasificacion(grupo, fase) {
    const directas = grupo.clasifican > 0
      ? `Pasan ${grupo.clasifican === 1 ? "el primero" : `los ${grupo.clasifican} primeros`} de este grupo.`
      : "";
    if (!fase.repesca || !grupo.enRepesca) return directas;
    const extra = fase.repesca === 1
      ? "Una plaza más se decide entre los mejores clasificados que quedan justo fuera."
      : `${fase.repesca} plazas más se deciden entre los mejores clasificados que quedan justo fuera.`;
    return `${directas} ${extra}`.trim();
  }
```

- [ ] **Step 3: Añadir el color en `global.css`**

Sustituye el bloque de `.is-clasificado` (líneas 4928-4936) por:

```css
/*
 * Quién pasa de ronda es la lectura principal de una clasificación, y pasar
 * directo no es lo mismo que pasar por repesca: dos colores, no uno.
 */
.torneo-tabla tbody tr.is-directo {
  background: rgba(183, 230, 91, 0.18);
}

.torneo-tabla tbody tr.is-directo td:first-child {
  box-shadow: inset 3px 0 0 var(--sea);
  font-weight: 800;
}

.torneo-tabla tbody tr.is-repesca {
  background: rgba(255, 217, 93, 0.32);
}

.torneo-tabla tbody tr.is-repesca td:first-child {
  box-shadow: inset 3px 0 0 #c79a1f;
  font-weight: 800;
}
```

Los valores están verificados contra `src/styles/global.css:4,7,9`: `--sea: #1c8f94`, `--lime: #b7e65b`, `--sun: #ffd95d`. El `rgba` de repesca es `--sun` (255, 217, 93) y va más opaco que el verde porque un amarillo claro sobre crema se pierde. El borde no usa `--sun` directamente: a 3px sobre crema no se distingue, así que lleva un ámbar oscurecido. **Compruébalo en el paso 4** y súbelo si sigue sin leerse.

- [ ] **Step 4: Verificar en el navegador, en móvil y en escritorio**

```bash
npm run build
```

Luego arranca el servidor con el binding de D1 y comprueba `/torneo/`:

```bash
npx wrangler dev --port 8788
```

Usa las herramientas del panel Browser: `preview_start` con la URL `http://localhost:8788/torneo/`, `read_page` para confirmar que las filas llevan `is-directo` / `is-repesca`, y `resize_window` con el preset `mobile` (375×812) para comprobar que la tabla sigue dentro de `.torneo-tabla-scroll` y que la página **no** hace scroll horizontal. Haz una captura de cada tamaño.

- [ ] **Step 5: Ejecutar la suite entera**

```bash
npm run verify
```

Esperado: build, `test:types` y los tres proyectos de test en verde.

- [ ] **Step 6: Commit**

```bash
git add public/assets/torneo-page.js src/styles/global.css
git commit -m "feat: /torneo/ pinta directos y repesca con colores distintos"
```

---

### Task 7: Programar el torneo 2026

**Files:**
- Create: `scripts/programar-torneo-2026.mjs`
- Create: `db/seeds/torneo-2026.sql` (generado por el script)

**Interfaces:**
- Consumes: todo lo anterior, ya desplegado o al menos aplicado en local.

- [ ] **Step 1: Crear la fase de grupos desde el panel**

Con `npx wrangler dev --port 8788` levantado y sesión de admin, en `/admin/torneo/`:

Fase nueva, clave `grupos`, nombre `Fase de grupos`, tipo `grupos`, orden `0`, **clasifican 2**, **repesca 1**. Reglas de partido: `sets 2`, `puntosPorSet 15`, `puntosSetDecisivo 15`, `diferencia 2`. Clasificación: `puntosVictoria 3`, `puntosDerrota 0`, `puntosVictoriaAjustada 2`, `puntosDerrotaAjustada 1`, desempates en este orden: puntos, enfrentamiento directo, ratio de sets, ratio de puntos.

- [ ] **Step 2: Crear los tres grupos**

- **Grupo A**, orden 0, sin cupo propio (hereda 2), en repesca **sí**, sin reglas propias.
- **Grupo B**, orden 1, sin cupo propio, en repesca **sí**, sin reglas propias.
- **Grupo C**, orden 2, cupo propio **3**, en repesca **no**, con reglas propias: `sets 1`, `puntosPorSet 21`, `diferencia 2`.

Después, en el diálogo de la fase no: el grupo C necesita **también** su bloque de clasificación, que el diálogo de grupo no expone. Aplícalo directamente:

```bash
npx wrangler d1 execute DB --local --command "UPDATE torneo_grupos SET reglas = json_set(reglas, '\$.clasificacion.puntosVictoria', 3, '\$.clasificacion.puntosDerrota', 0, '\$.clasificacion.puntosVictoriaAjustada', 3, '\$.clasificacion.puntosDerrotaAjustada', 0) WHERE nombre = 'C'"
```

Sin esto, cada victoria del grupo C valdría 2 puntos y cada derrota 1: con `sets: 1`, `setsMaximos` vale 1 y `torneo-vista.ts:158` marca todos sus partidos como resueltos en el set decisivo.

- [ ] **Step 3: Asignar los 13 equipos**

| Grupo A · Sáb 01/08 | Grupo B · Dom 02/08 | Grupo C · Lun 03/08 |
|---|---|---|
| Calvos de Orion | Limens | Showtime |
| Bye Bye Bye | Los Julais | Dosilva |
| Free Copa Arena | Segarro | Kylian dictador |
| Croquetillas de Arena | Deportivo A Silva | ONDA BRAVA |
| | | Alejo Mouris |

- [ ] **Step 4: Generar el calendario**

Botón «Generar calendario» de la fase. Comprueba que salen **22** partidos:

```bash
npx wrangler d1 execute DB --local --command "SELECT COUNT(*) AS n FROM partidos WHERE fase_id = (SELECT id FROM torneo_fases WHERE clave = 'grupos')"
```

Esperado: `n = 22` (6 + 6 + 10).

- [ ] **Step 5: Escribir el script de horarios**

Crea `scripts/programar-torneo-2026.mjs`:

```js
/*
 * Genera el SQL de horarios del torneo 2026 a partir de los partidos que ya
 * existen. No inventa ids: los lee, para que el mismo fichero valga en local y
 * en produccion sin editar nada a mano.
 *
 *   node scripts/programar-torneo-2026.mjs --local  > db/seeds/torneo-2026.sql
 *   node scripts/programar-torneo-2026.mjs --remote > db/seeds/torneo-2026.sql
 */
import { execFileSync } from "node:child_process";

const destino = process.argv.includes("--remote") ? "--remote" : "--local";

const consultar = (sql) => {
  const salida = execFileSync(
    "npx",
    ["--no-install", "wrangler", "d1", "execute", "DB", destino, "--json", "--command", sql],
    { encoding: "utf8", shell: true }
  );
  return JSON.parse(salida)[0].results;
};

// Cada grupo juega su tarde, y la eliminatoria sus dos. Los huecos son mas
// largos donde se juega al mejor de tres.
const TARDES = [
  { grupo: "A", fecha: "2026-08-01", inicio: "16:30", huecoMin: 50 },
  { grupo: "B", fecha: "2026-08-02", inicio: "16:30", huecoMin: 50 },
  { grupo: "C", fecha: "2026-08-03", inicio: "16:30", huecoMin: 30 }
];
const PISTA = "Pista central";

const conHueco = (fecha, inicio, minutos, indice) => {
  const base = new Date(`${fecha}T${inicio}:00`);
  base.setMinutes(base.getMinutes() + minutos * indice);
  const dosDigitos = (n) => String(n).padStart(2, "0");
  return `${fecha}T${dosDigitos(base.getHours())}:${dosDigitos(base.getMinutes())}`;
};

const lineas = [
  "-- Horarios y pista del torneo 2026. Generado por scripts/programar-torneo-2026.mjs.",
  "-- Una sola pista: los partidos van en serie, en el orden que dio el calendario.",
  ""
];

for (const tarde of TARDES) {
  const partidos = consultar(
    `SELECT p.id FROM partidos p
       JOIN torneo_grupos g ON g.id = p.grupo_id
      WHERE g.nombre = '${tarde.grupo}'
      ORDER BY p.sort_order ASC`
  );
  lineas.push(`-- Grupo ${tarde.grupo} — ${tarde.fecha}, ${partidos.length} partidos`);
  partidos.forEach((partido, indice) => {
    const cuando = conHueco(tarde.fecha, tarde.inicio, tarde.huecoMin, indice);
    lineas.push(
      `UPDATE partidos SET scheduled_at = '${cuando}', pista = '${PISTA}' WHERE id = '${partido.id}';`
    );
  });
  lineas.push("");
}

// La eliminatoria: 4 cuartos el sabado, y el domingo las dos semis, el tercer
// puesto y la final, en ese orden. El tercer puesto va antes que la final y
// despues de las dos semis, que es lo unico que el orden tiene que garantizar.
const cuadro = consultar(
  `SELECT p.id, p.ronda, p.ronda_orden, p.posicion FROM partidos p
     JOIN torneo_fases f ON f.id = p.fase_id
    WHERE f.clave = 'cuadro'
    ORDER BY p.ronda_orden ASC, p.posicion ASC`
);
const cuartos = cuadro.filter((p) => p.ronda_orden === 0);
const resto = cuadro.filter((p) => p.ronda_orden > 0);
const ordenDomingo = [
  ...resto.filter((p) => p.ronda === "Semifinales"),
  ...resto.filter((p) => p.ronda === "Tercer puesto"),
  ...resto.filter((p) => p.ronda === "Final")
];

lineas.push(`-- Cuartos — 2026-08-08, ${cuartos.length} partidos`);
cuartos.forEach((partido, indice) => {
  lineas.push(
    `UPDATE partidos SET scheduled_at = '${conHueco("2026-08-08", "17:00", 60, indice)}', pista = '${PISTA}' WHERE id = '${partido.id}';`
  );
});
lineas.push("");
lineas.push(`-- Semis, tercer puesto y final — 2026-08-09, ${ordenDomingo.length} partidos`);
ordenDomingo.forEach((partido, indice) => {
  lineas.push(
    `UPDATE partidos SET scheduled_at = '${conHueco("2026-08-09", "17:00", 60, indice)}', pista = '${PISTA}' WHERE id = '${partido.id}';`
  );
});

console.log(lineas.join("\n"));
```

- [ ] **Step 6: Crear la fase eliminatoria y su cuadro**

En el panel: fase nueva, clave `cuadro`, nombre `Eliminatoria`, tipo `eliminatoria`, orden `1`, clasifican `0`, repesca `0`. Reglas: `sets 2`, `puntosPorSet 21`, `puntosSetDecisivo 15`, `diferencia 2`.

Luego «Generar cuadro» con tamaño **8** y la casilla **«con 3.er puesto» marcada**. Comprueba que salen 8 partidos:

```bash
npx wrangler d1 execute DB --local --command "SELECT ronda, COUNT(*) AS n FROM partidos WHERE fase_id = (SELECT id FROM torneo_fases WHERE clave = 'cuadro') GROUP BY ronda"
```

Esperado: Cuartos de final 4, Semifinales 2, Tercer puesto 1, Final 1.

- [ ] **Step 7: Generar y aplicar los horarios en local**

```bash
node scripts/programar-torneo-2026.mjs --local > db/seeds/torneo-2026.sql
```

Revisa el fichero: tienen que salir 30 `UPDATE` (22 de grupos + 8 de cuadro). Luego:

```bash
npx wrangler d1 execute DB --local --file db/seeds/torneo-2026.sql
```

Y comprueba que ninguna tarde se pasa de las 21:30:

```bash
npx wrangler d1 execute DB --local --command "SELECT substr(scheduled_at,1,10) AS dia, COUNT(*) AS partidos, MIN(substr(scheduled_at,12,5)) AS primero, MAX(substr(scheduled_at,12,5)) AS ultimo FROM partidos WHERE scheduled_at IS NOT NULL GROUP BY dia ORDER BY dia"
```

Esperado: 01/08 → 6 partidos, 16:30-20:40. 02/08 → 6, 16:30-20:40. 03/08 → 10, 16:30-21:00. 08/08 → 4, 17:00-20:00. 09/08 → 4, 17:00-20:00.

- [ ] **Step 8: Verificar `/torneo/` con el torneo real cargado**

Con `npx wrangler dev --port 8788`, abre `/torneo/` y comprueba con `read_page` que se ven las tres fases, los 13 equipos repartidos y las notas de clasificación correctas. Captura en móvil y en escritorio.

- [ ] **Step 9: Commit**

```bash
git add scripts/programar-torneo-2026.mjs db/seeds/torneo-2026.sql
git commit -m "feat: script de horarios y calendario del torneo 2026"
```

- [ ] **Step 10: Parar y preguntar antes de tocar producción**

**No ejecutes nada contra producción.** Resume al usuario qué quedó verificado en local y pregunta explícitamente si quiere que se replique en producción, advirtiendo de que «Generar calendario» **borra los partidos de esa fase** (`torneo.ts:179`) y de que la migración 0022 hay que aplicarla antes con `npx wrangler d1 migrations apply DB --remote`.

---

## Notas para quien ejecute

**`test/integration/setup.ts` no hay que tocarlo.** La migración 0022 añade columnas, no tablas, así que su lista `TABLAS` sigue completa. Compruébalo igualmente al aplicar la migración: si algún test empieza a fallar según el orden en que se ejecute, es señal de que algo nuevo no se está limpiando entre tests.


**Un cruce del mismo grupo en cuartos es posible y no lo arregla este plan.** Con el cupo 2/2/3 y una plaza de repesca, el orden de siembra queda `A1, B1, C1, A2, B2, C2, C3, repesca`, y `sembrarEliminatoria` empareja el primero con el último: el tercer cuarto de final sale `C1` contra `C2`, dos equipos que acaban de jugar entre sí en el grupo. Evitarlo de forma general es un algoritmo de asignación con restricciones, no un cambio de una línea, y está fuera del alcance acordado. Si el usuario lo quiere, es un plan aparte; mientras tanto se corrige a mano desde el panel, que para eso `origen_equipo_*` distingue `'manual'` y `propagarResultado` no lo pisa.
